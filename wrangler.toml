name = "ai-youtube-summarizer"
main = "src/index.ts"
compatibility_date = "2024-04-05"
compatibility_flags = ["nodejs_compat"]

# We will use an in-memory fallback for the KV, but we also define a KV binding for production.
# To use KV, uncomment the lines below and replace the id with your KV namespace ID.
# [[kv_namespaces]]
# binding = "SESSION_KV"
# id = "YOUR_KV_NAMESPACE_ID"
